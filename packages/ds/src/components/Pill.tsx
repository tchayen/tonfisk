import React, { ReactElement } from "react";

type Props = {
  /**
   * Content of pill.
   */
  children: string;
};

/**
 * TODO
 */
export function Pill(props: Props): ReactElement {
  return <div>123</div>;
}
