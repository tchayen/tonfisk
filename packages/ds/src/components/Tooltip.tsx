import { ReactElement } from "react";

type Props = {
  /**
   * Content of tooltip.
   */
  children: string;
};

/**
 * Tooltip component.
 */
export function Tooltip(props: Props): ReactElement {
  return null;
}
