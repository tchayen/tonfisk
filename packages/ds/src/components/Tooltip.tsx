import React, { ReactElement } from "react";

type Props = {
  /**
   * Content of tooltip.
   */
  children: string;
};

/**
 * Tooltip component.
 */
export function Tooltip(props: Props): ReactElement {
  return <div>123</div>;
}
