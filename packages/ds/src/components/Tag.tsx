import React, { ReactElement } from "react";

type Props = {
  /**
   * Content of tag.
   */
  children: string;
};

/**
 * TODO
 */
export function Tag(props: Props): ReactElement {
  return <div>123</div>;
}
